'use client';
import React from 'react';

function AddToCart() {
    return (
        <div>
            <button onClick={() => console.log('Click')}>Ajouter au panier</button>
        </div>
    );
}

export default AddToCart;